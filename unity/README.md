# Unity project goes here

Created via Unity Hub, not committed as a template — this README is just a placeholder so the folder exists in git.

Owner: Shakila (Unity Digital Twin) — see `docs/team-briefs/SHAKILA.md`, which includes a working starter script for polling the backend and driving the scene.

When you create the Unity project, put it directly in this `unity/` folder so the root `.gitignore`'s Unity rules (`Library/`, `Temp/`, `Obj/`, `Build/`, etc.) apply automatically.
